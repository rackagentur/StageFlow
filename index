<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Stageflow — From Outreach to Booked</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Mono:wght@400;500&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --bg:       #080808;
      --surface:  #111111;
      --border:   #1a1a1a;
      --purple:   #7B3FE4;
      --purple-l: #9B5FFF;
      --purple-bg:#1A0F2E;
      --gold:     #D4AF37;
      --text:     #F0F0F0;
      --muted:    #555;
      --dim:      #888;
      --green:    #22C55E;
      --red:      #EF4444;
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    html { scroll-behavior: smooth; }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'DM Sans', sans-serif;
      font-size: 16px;
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* ── Grain overlay ── */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
      opacity: 0.025;
      pointer-events: none;
      z-index: 9999;
    }

    /* ── Typography ── */
    h1, h2, h3, h4 { font-family: 'Syne', sans-serif; line-height: 1.1; }
    .mono { font-family: 'DM Mono', monospace; }

    /* ── Layout ── */
    .container { max-width: 1080px; margin: 0 auto; padding: 0 32px; }

    /* ── Nav ── */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      padding: 20px 32px;
      display: flex; justify-content: space-between; align-items: center;
      border-bottom: 1px solid transparent;
      transition: border-color 0.3s, background 0.3s;
    }
    nav.scrolled { background: rgba(8,8,8,0.92); backdrop-filter: blur(12px); border-color: var(--border); }
    .nav-logo { display: flex; align-items: center; gap: 12px; text-decoration: none; }
    .nav-logo-bars { display: flex; align-items: flex-end; gap: 3px; height: 22px; }
    .nav-logo-bars span { display: block; width: 4px; border-radius: 2px; background: var(--purple); }
    .nav-logo-bars span:nth-child(1) { height: 40%; opacity: 0.45; }
    .nav-logo-bars span:nth-child(2) { height: 65%; opacity: 0.6; }
    .nav-logo-bars span:nth-child(3) { height: 82%; opacity: 0.8; }
    .nav-logo-bars span:nth-child(4) { height: 100%; box-shadow: 0 0 8px var(--purple); }
    .nav-wordmark { font-family: 'Syne', sans-serif; font-weight: 800; font-size: 17px; letter-spacing: -0.02em; color: var(--text); }
    .nav-links { display: flex; align-items: center; gap: 28px; }
    .nav-links a { color: var(--dim); text-decoration: none; font-size: 13px; font-weight: 500; transition: color 0.15s; }
    .nav-links a:hover { color: var(--text); }
    .btn-nav { padding: 8px 20px; background: var(--purple); border-radius: 8px; color: #fff !important; font-weight: 700 !important; font-size: 13px !important; transition: opacity 0.15s !important; }
    .btn-nav:hover { opacity: 0.85; }

    /* ── Buttons ── */
    .btn-primary {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 15px 32px;
      background: linear-gradient(135deg, var(--purple), var(--purple-l));
      color: #fff; font-family: 'Syne', sans-serif; font-weight: 700; font-size: 15px;
      border: none; border-radius: 12px; cursor: pointer; text-decoration: none;
      box-shadow: 0 6px 30px rgba(123,63,228,0.4);
      transition: transform 0.15s, box-shadow 0.15s;
      letter-spacing: 0.01em;
    }
    .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 10px 40px rgba(123,63,228,0.5); }

    .btn-ghost {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 15px 28px;
      background: transparent; color: var(--dim);
      font-size: 14px; font-weight: 500;
      border: 1px solid var(--border); border-radius: 12px; cursor: pointer; text-decoration: none;
      transition: color 0.15s, border-color 0.15s;
    }
    .btn-ghost:hover { color: var(--text); border-color: #333; }

    /* ── Hero ── */
    .hero {
      min-height: 100vh;
      display: flex; flex-direction: column; justify-content: center;
      padding: 120px 32px 80px;
      position: relative; overflow: hidden;
    }

    /* Radial glow */
    .hero::after {
      content: '';
      position: absolute;
      top: -20%; left: 50%; transform: translateX(-50%);
      width: 900px; height: 600px;
      background: radial-gradient(ellipse, rgba(123,63,228,0.12) 0%, transparent 70%);
      pointer-events: none;
    }

    /* Vertical grid lines */
    .hero-grid {
      position: absolute; inset: 0; pointer-events: none;
      background-image:
        repeating-linear-gradient(90deg, rgba(255,255,255,0.02) 0, rgba(255,255,255,0.02) 1px, transparent 1px, transparent 120px);
    }

    .hero-inner { max-width: 1080px; margin: 0 auto; position: relative; z-index: 1; }

    .hero-label {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 6px 14px;
      background: var(--purple-bg); border: 1px solid rgba(123,63,228,0.3);
      border-radius: 20px; font-size: 11px; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase; color: var(--purple-l);
      margin-bottom: 32px;
    }
    .hero-label-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--purple-l); box-shadow: 0 0 6px var(--purple); animation: pulse 2s infinite; }
    @keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.4; } }

    .hero h1 {
      font-size: clamp(52px, 7vw, 88px);
      font-weight: 800;
      letter-spacing: -0.04em;
      line-height: 1.0;
      margin-bottom: 28px;
    }
    .hero h1 em { font-style: normal; color: var(--purple-l); }
    .hero h1 .strike {
      position: relative; color: var(--muted);
      text-decoration: none;
    }
    .hero h1 .strike::after {
      content: '';
      position: absolute; left: 0; right: 0;
      top: 50%; height: 3px;
      background: var(--red);
      transform: rotate(-2deg);
    }

    .hero-sub {
      font-size: clamp(16px, 2vw, 20px);
      color: var(--dim); max-width: 560px;
      margin-bottom: 44px; line-height: 1.7;
      font-weight: 400;
    }
    .hero-sub strong { color: var(--text); font-weight: 600; }

    .hero-cta { display: flex; gap: 14px; align-items: center; flex-wrap: wrap; margin-bottom: 64px; }

    .hero-social-proof {
      display: flex; align-items: center; gap: 14px;
      font-size: 12px; color: var(--muted);
    }
    .hero-avatars { display: flex; }
    .hero-avatars span {
      width: 30px; height: 30px; border-radius: 50%;
      border: 2px solid var(--bg);
      background: var(--surface);
      display: flex; align-items: center; justify-content: center;
      font-size: 12px; margin-left: -8px;
    }
    .hero-avatars span:first-child { margin-left: 0; }

    /* ── Stat bar ── */
    .stat-bar {
      display: flex; gap: 0;
      border: 1px solid var(--border); border-radius: 14px; overflow: hidden;
      margin-top: 80px;
    }
    .stat-item {
      flex: 1; padding: 28px 32px;
      border-right: 1px solid var(--border);
    }
    .stat-item:last-child { border-right: none; }
    .stat-num {
      font-family: 'DM Mono', monospace; font-size: 36px; font-weight: 500;
      color: var(--text); letter-spacing: -0.03em; margin-bottom: 4px;
    }
    .stat-num span { color: var(--purple); }
    .stat-label { font-size: 12px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.08em; font-weight: 600; }

    /* ── Section ── */
    section { padding: 100px 0; }
    .section-label {
      font-size: 10px; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase;
      color: var(--purple); margin-bottom: 16px;
    }
    .section-title {
      font-size: clamp(32px, 4vw, 52px); font-weight: 800;
      letter-spacing: -0.03em; margin-bottom: 20px; line-height: 1.1;
    }
    .section-sub { font-size: 17px; color: var(--dim); max-width: 520px; line-height: 1.7; margin-bottom: 56px; }

    /* ── Problem section ── */
    .problem-grid {
      display: grid; grid-template-columns: 1fr 1fr; gap: 2px;
      border: 1px solid var(--border); border-radius: 16px; overflow: hidden;
      background: var(--border);
    }
    .problem-card {
      background: var(--surface); padding: 36px;
      position: relative; overflow: hidden;
    }
    .problem-card::before {
      content: '';
      position: absolute; top: 0; left: 0; right: 0; height: 2px;
    }
    .problem-card.red::before   { background: var(--red); }
    .problem-card.green::before { background: var(--green); }
    .problem-card.amber::before { background: var(--gold); }
    .problem-card.purple::before{ background: var(--purple); }

    .problem-icon { font-size: 28px; margin-bottom: 16px; }
    .problem-title { font-family: 'Syne', sans-serif; font-size: 18px; font-weight: 700; margin-bottom: 10px; }
    .problem-desc  { font-size: 14px; color: var(--dim); line-height: 1.7; }

    /* ── Pipeline visual ── */
    .pipeline-visual {
      background: var(--surface); border: 1px solid var(--border);
      border-radius: 16px; padding: 32px; overflow: hidden;
    }
    .pipeline-track {
      display: flex; gap: 8px; overflow-x: auto; padding-bottom: 4px;
      scrollbar-width: none;
    }
    .pipeline-track::-webkit-scrollbar { display: none; }
    .pipeline-col { min-width: 160px; flex-shrink: 0; }
    .pipeline-col-head {
      font-size: 10px; font-weight: 700; letter-spacing: 0.08em;
      text-transform: uppercase; margin-bottom: 10px;
      display: flex; align-items: center; gap: 6px;
    }
    .pipeline-col-dot { width: 7px; height: 7px; border-radius: 50%; }
    .pipeline-card {
      background: var(--bg); border: 1px solid var(--border);
      border-radius: 9px; padding: 12px 14px; margin-bottom: 8px;
    }
    .pipeline-card-name { font-size: 12px; font-weight: 700; color: var(--text); margin-bottom: 4px; }
    .pipeline-card-tag {
      display: inline-block; font-size: 9px; font-weight: 700;
      padding: 2px 7px; border-radius: 4px; letter-spacing: 0.06em; text-transform: uppercase;
    }
    .pipeline-card.overdue { border-color: rgba(212,175,55,0.4); }
    .pipeline-card.overdue .pipeline-card-name::after { content: ' ⏰'; font-size: 10px; }

    /* ── Features ── */
    .features-grid {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px;
      border: 1px solid var(--border); border-radius: 16px; overflow: hidden;
      background: var(--border);
    }
    .feature-card {
      background: var(--surface); padding: 36px 32px;
      transition: background 0.2s;
    }
    .feature-card:hover { background: #161616; }
    .feature-icon {
      width: 44px; height: 44px; border-radius: 11px;
      display: flex; align-items: center; justify-content: center;
      font-size: 20px; margin-bottom: 20px;
      background: var(--purple-bg); border: 1px solid rgba(123,63,228,0.25);
    }
    .feature-title { font-family: 'Syne', sans-serif; font-size: 17px; font-weight: 700; margin-bottom: 10px; }
    .feature-desc  { font-size: 14px; color: var(--dim); line-height: 1.7; }

    /* ── Testimonial ── */
    .testimonial-section { background: var(--surface); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
    .testimonial-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; background: var(--border); }
    .testimonial-card { background: var(--surface); padding: 36px 32px; }
    .testimonial-quote { font-size: 15px; color: var(--text); line-height: 1.75; margin-bottom: 24px; font-style: italic; }
    .testimonial-author { display: flex; align-items: center; gap: 12px; }
    .testimonial-avatar {
      width: 38px; height: 38px; border-radius: 50%;
      background: var(--purple-bg); border: 1px solid rgba(123,63,228,0.3);
      display: flex; align-items: center; justify-content: center; font-size: 14px; flex-shrink: 0;
    }
    .testimonial-name { font-size: 13px; font-weight: 700; color: var(--text); }
    .testimonial-role { font-size: 11px; color: var(--muted); margin-top: 1px; }

    /* ── Pricing ── */
    .pricing-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; max-width: 760px; margin: 0 auto; }
    .pricing-card {
      border: 1px solid var(--border); border-radius: 18px; overflow: hidden;
      position: relative;
    }
    .pricing-card.pro { border-color: rgba(123,63,228,0.5); }
    .pricing-card.pro::before {
      content: '';
      position: absolute; top: 0; left: 0; right: 0; height: 2px;
      background: linear-gradient(90deg, var(--purple), var(--purple-l), var(--gold));
    }
    .pricing-head { padding: 28px 28px 24px; border-bottom: 1px solid var(--border); }
    .pricing-tier { font-family: 'DM Mono', monospace; font-size: 11px; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--muted); margin-bottom: 16px; }
    .pricing-price { display: flex; align-items: baseline; gap: 4px; margin-bottom: 6px; }
    .pricing-price-num { font-family: 'Syne', sans-serif; font-size: 44px; font-weight: 800; letter-spacing: -0.04em; }
    .pricing-price-per { font-size: 14px; color: var(--dim); }
    .pricing-price-annual { font-size: 12px; color: var(--green); }
    .pricing-body { padding: 24px 28px 28px; }
    .pricing-features { list-style: none; display: flex; flex-direction: column; gap: 12px; margin-bottom: 28px; }
    .pricing-features li { display: flex; align-items: flex-start; gap: 10px; font-size: 13px; color: var(--dim); }
    .pricing-features li::before { content: '✓'; color: var(--green); font-weight: 800; font-size: 12px; flex-shrink: 0; margin-top: 1px; }
    .pricing-features li.locked { color: var(--muted); }
    .pricing-features li.locked::before { content: '—'; color: var(--muted); }

    /* ── How it works ── */
    .steps { display: flex; flex-direction: column; gap: 2px; border: 1px solid var(--border); border-radius: 16px; overflow: hidden; background: var(--border); max-width: 720px; }
    .step { background: var(--surface); padding: 32px 36px; display: flex; gap: 28px; align-items: flex-start; }
    .step-num {
      width: 36px; height: 36px; border-radius: 9px; flex-shrink: 0;
      background: var(--purple-bg); border: 1px solid rgba(123,63,228,0.3);
      display: flex; align-items: center; justify-content: center;
      font-family: 'DM Mono', monospace; font-size: 13px; font-weight: 500; color: var(--purple-l);
    }
    .step-title { font-family: 'Syne', sans-serif; font-size: 16px; font-weight: 700; margin-bottom: 6px; }
    .step-desc  { font-size: 14px; color: var(--dim); line-height: 1.7; }

    /* ── CTA section ── */
    .cta-section {
      text-align: center; padding: 120px 32px;
      position: relative; overflow: hidden;
    }
    .cta-section::before {
      content: '';
      position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%);
      width: 700px; height: 400px;
      background: radial-gradient(ellipse, rgba(123,63,228,0.1) 0%, transparent 70%);
      pointer-events: none;
    }
    .cta-section h2 { font-size: clamp(36px, 5vw, 64px); font-weight: 800; letter-spacing: -0.04em; margin-bottom: 20px; line-height: 1.1; }
    .cta-section h2 em { font-style: normal; color: var(--purple-l); }
    .cta-section p { font-size: 18px; color: var(--dim); margin-bottom: 40px; }
    .cta-buttons { display: flex; gap: 14px; justify-content: center; align-items: center; flex-wrap: wrap; }
    .cta-note { font-size: 12px; color: var(--muted); margin-top: 20px; }

    /* ── Footer ── */
    footer {
      border-top: 1px solid var(--border); padding: 40px 32px;
      display: flex; justify-content: space-between; align-items: center;
      flex-wrap: wrap; gap: 16px;
    }
    .footer-brand { display: flex; align-items: center; gap: 10px; }
    .footer-brand span { font-family: 'Syne', sans-serif; font-size: 15px; font-weight: 700; }
    .footer-tagline { font-size: 12px; color: var(--muted); }
    .footer-links { display: flex; gap: 24px; }
    .footer-links a { font-size: 12px; color: var(--muted); text-decoration: none; transition: color 0.15s; }
    .footer-links a:hover { color: var(--text); }

    /* ── Animations ── */
    .fade-up { opacity: 0; transform: translateY(24px); transition: opacity 0.6s ease, transform 0.6s ease; }
    .fade-up.visible { opacity: 1; transform: translateY(0); }

    /* ── Mobile ── */
    @media (max-width: 768px) {
      nav { padding: 16px 20px; }
      .nav-links { display: none; }
      .hero { padding: 100px 20px 60px; }
      .container { padding: 0 20px; }
      section { padding: 70px 0; }
      .problem-grid, .features-grid, .testimonial-grid, .pricing-grid { grid-template-columns: 1fr; }
      .stat-bar { flex-direction: column; }
      .stat-item { border-right: none; border-bottom: 1px solid var(--border); }
      .stat-item:last-child { border-bottom: none; }
      footer { flex-direction: column; align-items: flex-start; }
    }
  </style>
</head>
<body>

  <!-- Nav -->
  <nav id="nav">
    <a href="#" class="nav-logo">
      <div class="nav-logo-bars">
        <span></span><span></span><span></span><span></span>
      </div>
      <span class="nav-wordmark">Stageflow</span>
    </a>
    <div class="nav-links">
      <a href="#how">How it works</a>
      <a href="#features">Features</a>
      <a href="#pricing">Pricing</a>
      <a href="#" class="btn-nav">Start free</a>
    </div>
  </nav>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-grid"></div>
    <div class="hero-inner">
      <div class="hero-label">
        <span class="hero-label-dot"></span>
        Booking OS for DJs
      </div>

      <h1>
        DJs don't get<br>
        <span class="strike">rejected</span><br>
        They get <em>ignored.</em>
      </h1>

      <p class="hero-sub">
        <strong>Stageflow</strong> is the booking system that keeps you following up, tracking every outreach, and moving leads from cold contact to confirmed gig — consistently.
      </p>

      <div class="hero-cta">
        <a href="#" class="btn-primary">Start for free →</a>
        <a href="#how" class="btn-ghost">See how it works</a>
      </div>

      <div class="hero-social-proof">
        <div class="hero-avatars">
          <span>🎧</span><span>🎛️</span><span>🎵</span><span>🔊</span>
        </div>
        <span>Used by independent DJs across 12 cities</span>
      </div>

      <!-- Stat bar -->
      <div class="stat-bar fade-up">
        <div class="stat-item">
          <div class="stat-num">73<span>%</span></div>
          <div class="stat-label">of DJs never follow up after first message</div>
        </div>
        <div class="stat-item">
          <div class="stat-num">3<span>×</span></div>
          <div class="stat-label">more replies with consistent follow-up</div>
        </div>
        <div class="stat-item">
          <div class="stat-num">€9</div>
          <div class="stat-label">one extra booking pays for years of Pro</div>
        </div>
        <div class="stat-item">
          <div class="stat-num mono">∞</div>
          <div class="stat-label">leads on Pro — no ceiling</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Problem -->
  <section style="padding: 80px 0;">
    <div class="container">
      <p class="section-label">The real problem</p>
      <h2 class="section-title">Your booking career is dying in someone's inbox</h2>
      <p class="section-sub">You sent the message. They didn't reply. You moved on. That's the whole story — and it's costing you gigs.</p>

      <div class="problem-grid fade-up">
        <div class="problem-card red">
          <div class="problem-icon">✉️</div>
          <div class="problem-title">You send one message and stop</div>
          <div class="problem-desc">Most venues need 2–3 touches before they respond. The DJ who follows up gets the slot. The one who doesn't gets forgotten.</div>
        </div>
        <div class="problem-card amber">
          <div class="problem-icon">🗂️</div>
          <div class="problem-title">You have no system</div>
          <div class="problem-desc">Notes app, DMs, spreadsheet, sticky notes — you don't actually know who you've contacted, when, or what to do next.</div>
        </div>
        <div class="problem-card purple">
          <div class="problem-icon">⏰</div>
          <div class="problem-title">Follow-ups fall through the cracks</div>
          <div class="problem-desc">You meant to follow up. Life happened. Two weeks later the slot went to someone who was more persistent — not more talented.</div>
        </div>
        <div class="problem-card green">
          <div class="problem-icon">📈</div>
          <div class="problem-title">Stageflow fixes all three</div>
          <div class="problem-desc">A pipeline that tracks every lead, auto-reminders so you never miss a follow-up, and outreach templates built for the scene.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- How it works -->
  <section id="how">
    <div class="container">
      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: start;">
        <div>
          <p class="section-label">How it works</p>
          <h2 class="section-title">One weekly loop. That's it.</h2>
          <p class="section-sub">The DJ who gets booked isn't more talented. They're more consistent. Stageflow makes consistency automatic.</p>

          <div class="steps fade-up">
            <div class="step">
              <div class="step-num">01</div>
              <div>
                <div class="step-title">Add leads</div>
                <div class="step-desc">Drop in venues, promoters, and festivals. Tier them by priority — A1 dream gigs down to long shots.</div>
              </div>
            </div>
            <div class="step">
              <div class="step-num">02</div>
              <div>
                <div class="step-title">Send outreach</div>
                <div class="step-desc">Use templates built for the scene — underground, circuit, Disco, or a warm intro through a mutual connection.</div>
              </div>
            </div>
            <div class="step">
              <div class="step-num">03</div>
              <div>
                <div class="step-title">Get reminded to follow up</div>
                <div class="step-desc">Stageflow auto-schedules follow-up reminders at 5 and 14 days. You never have to remember — it tells you who to contact today.</div>
              </div>
            </div>
            <div class="step">
              <div class="step-num">04</div>
              <div>
                <div class="step-title">Close the booking</div>
                <div class="step-desc">When they reply, move them to Replied. Confirm the deal, add it to your gig calendar. Done.</div>
              </div>
            </div>
          </div>
        </div>

        <!-- Pipeline preview -->
        <div class="fade-up" style="position: sticky; top: 100px;">
          <div class="pipeline-visual">
            <div style="font-size: 11px; color: var(--muted); letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 20px; font-weight: 700;">Live Pipeline</div>
            <div class="pipeline-track">

              <div class="pipeline-col">
                <div class="pipeline-col-head">
                  <div class="pipeline-col-dot" style="background:#444"></div>
                  <span style="color:var(--muted)">Target</span>
                </div>
                <div class="pipeline-card">
                  <div class="pipeline-card-name">Tresor Berlin</div>
                  <span class="pipeline-card-tag" style="background:rgba(123,63,228,0.2);color:#9B5FFF">Tech-House</span>
                </div>
                <div class="pipeline-card">
                  <div class="pipeline-card-name">De School AMS</div>
                  <span class="pipeline-card-tag" style="background:rgba(123,63,228,0.2);color:#9B5FFF">Tech-House</span>
                </div>
              </div>

              <div class="pipeline-col">
                <div class="pipeline-col-head">
                  <div class="pipeline-col-dot" style="background:#7B3FE4"></div>
                  <span style="color:var(--muted)">Contacted</span>
                </div>
                <div class="pipeline-card">
                  <div class="pipeline-card-name">Fabric London</div>
                  <span class="pipeline-card-tag" style="background:rgba(59,130,246,0.2);color:#60A5FA">Disco</span>
                </div>
              </div>

              <div class="pipeline-col">
                <div class="pipeline-col-head">
                  <div class="pipeline-col-dot" style="background:#9B5FFF"></div>
                  <span style="color:var(--muted)">Follow-up 1</span>
                </div>
                <div class="pipeline-card overdue">
                  <div class="pipeline-card-name">Output NYC</div>
                  <span class="pipeline-card-tag" style="background:rgba(212,175,55,0.15);color:#D4AF37">Due today</span>
                </div>
              </div>

              <div class="pipeline-col">
                <div class="pipeline-col-head">
                  <div class="pipeline-col-dot" style="background:#22C55E"></div>
                  <span style="color:var(--muted)">Replied</span>
                </div>
                <div class="pipeline-card" style="border-color: rgba(34,197,94,0.3);">
                  <div class="pipeline-card-name">BPM Festival</div>
                  <span class="pipeline-card-tag" style="background:rgba(34,197,94,0.15);color:#22C55E">Hot 🔥</span>
                </div>
              </div>

              <div class="pipeline-col">
                <div class="pipeline-col-head">
                  <div class="pipeline-col-dot" style="background:#D4AF37; box-shadow: 0 0 6px #D4AF37;"></div>
                  <span style="color:var(--gold)">Booked</span>
                </div>
                <div class="pipeline-card" style="border-color:rgba(212,175,55,0.3)">
                  <div class="pipeline-card-name">Alegria NYC</div>
                  <span class="pipeline-card-tag" style="background:rgba(212,175,55,0.15);color:#D4AF37">Mar 15 ✓</span>
                </div>
              </div>

            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section id="features" style="background: var(--surface); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border);">
    <div class="container">
      <p class="section-label">Everything you need</p>
      <h2 class="section-title">Built for how DJs actually work</h2>
      <p class="section-sub">Not a generic CRM. Not a chatbot. A system designed around the booking loop.</p>

      <div class="features-grid fade-up">
        <div class="feature-card">
          <div class="feature-icon">⬛</div>
          <div class="feature-title">Booking Pipeline</div>
          <div class="feature-desc">Kanban-style board from Target to Booked. See every lead, every stage, at a glance. Move them forward with one tap.</div>
        </div>
        <div class="feature-card">
          <div class="feature-icon">⏰</div>
          <div class="feature-title">Auto Follow-up Reminders</div>
          <div class="feature-desc">Advance a lead to Contacted and Stageflow automatically schedules the next follow-up. You just show up when it tells you to.</div>
        </div>
        <div class="feature-card">
          <div class="feature-icon">✦</div>
          <div class="feature-title">Outreach Templates</div>
          <div class="feature-desc">Four templates built for the scene — underground Tech House, high-energy circuit, Disco soul, and the warm intro. Copy and personalise in seconds.</div>
        </div>
        <div class="feature-card">
          <div class="feature-icon">✉</div>
          <div class="feature-title">Reply Hub</div>
          <div class="feature-desc">When venues write back, they land here. Read, draft your reply, and confirm the booking — all without leaving Stageflow.</div>
        </div>
        <div class="feature-card">
          <div class="feature-icon">📅</div>
          <div class="feature-title">Gig Calendar</div>
          <div class="feature-desc">Your confirmed and pending gigs on a visual calendar. Never double-book. Always know what's coming up and what's being negotiated.</div>
        </div>
        <div class="feature-card">
          <div class="feature-icon">▣</div>
          <div class="feature-title">Conversion Funnel</div>
          <div class="feature-desc">See exactly where leads are dropping off. Fix your weakest stage — whether that's initial contact, follow-up 1, or closing replied leads.</div>
        </div>
        <div class="feature-card">
          <div class="feature-icon">◇</div>
          <div class="feature-title">Artist Assets</div>
          <div class="feature-desc">Your EPK, mix links, bio, and press photos — all in one tab, ready to copy and paste into any outreach message.</div>
        </div>
        <div class="feature-card">
          <div class="feature-icon">⚙</div>
          <div class="feature-title">Your Genre, Your System</div>
          <div class="feature-desc">Add your own genres — Afrobeats, Drum & Bass, Reggaeton, anything. Stageflow adapts to your scene, not the other way around.</div>
        </div>
        <div class="feature-card" style="background: var(--purple-bg); border-left: 2px solid var(--purple);">
          <div class="feature-icon" style="background: rgba(123,63,228,0.3);">🔒</div>
          <div class="feature-title" style="color: var(--purple-l);">All data stays yours</div>
          <div class="feature-desc">Everything is saved locally in your browser. No account required to start. Pro adds cloud sync across devices.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonials -->
  <section class="testimonial-section">
    <div class="container">
      <p class="section-label" style="margin-bottom: 48px;">From the scene</p>
      <div class="testimonial-grid fade-up">
        <div class="testimonial-card">
          <div class="testimonial-quote">"I sent 40 messages over 3 months and got 2 replies. First week using Stageflow I followed up on 12 of them. Landed 3 bookings."</div>
          <div class="testimonial-author">
            <div class="testimonial-avatar">🎧</div>
            <div>
              <div class="testimonial-name">Marco V.</div>
              <div class="testimonial-role">Tech House · Berlin</div>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <div class="testimonial-quote">"The follow-up reminders alone are worth it. I used to rely on memory. I kept forgetting. Now the system tells me what to do and I just do it."</div>
          <div class="testimonial-author">
            <div class="testimonial-avatar">🎛️</div>
            <div>
              <div class="testimonial-name">Laila K.</div>
              <div class="testimonial-role">Disco · Amsterdam</div>
            </div>
          </div>
        </div>
        <div class="testimonial-card">
          <div class="testimonial-quote">"I've tried spreadsheets, Notion, even a CRM built for sales. Nothing fit until this. It speaks DJ. The pipeline makes sense immediately."</div>
          <div class="testimonial-author">
            <div class="testimonial-avatar">🔊</div>
            <div>
              <div class="testimonial-name">Dani R.</div>
              <div class="testimonial-role">Festival / Circuit · Madrid</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing -->
  <section id="pricing">
    <div class="container">
      <div style="text-align: center; margin-bottom: 56px;">
        <p class="section-label">Pricing</p>
        <h2 class="section-title">One booking pays for years</h2>
        <p class="section-sub" style="margin: 0 auto;">Start free. Upgrade when you're ready to scale — or when you hit the lead limit and realize you need more.</p>
      </div>

      <div class="pricing-grid fade-up">
        <!-- Free -->
        <div class="pricing-card">
          <div class="pricing-head">
            <div class="pricing-tier">Free</div>
            <div class="pricing-price">
              <span class="pricing-price-num">€0</span>
              <span class="pricing-price-per">forever</span>
            </div>
            <div style="font-size: 12px; color: var(--muted);">No credit card. No time limit.</div>
          </div>
          <div class="pricing-body">
            <ul class="pricing-features">
              <li>Up to 10 leads in pipeline</li>
              <li>Up to 5 gigs on calendar</li>
              <li>2 outreach templates (Berlin + Circuit)</li>
              <li>Reply Hub — unlimited</li>
              <li>Artist assets tab</li>
              <li>Search + filter</li>
              <li class="locked">Auto follow-up scheduling</li>
              <li class="locked">Conversion funnel + Next Actions</li>
              <li class="locked">Custom follow-up intervals</li>
              <li class="locked">Unlimited leads + gigs</li>
            </ul>
            <a href="#" class="btn-ghost" style="width: 100%; justify-content: center;">Start free</a>
          </div>
        </div>

        <!-- Pro -->
        <div class="pricing-card pro">
          <div class="pricing-head" style="background: var(--purple-bg);">
            <div class="pricing-tier" style="color: var(--purple-l);">Pro</div>
            <div class="pricing-price">
              <span class="pricing-price-num">€9</span>
              <span class="pricing-price-per">/month</span>
            </div>
            <div class="pricing-price-annual">€79/year — save 2 months</div>
          </div>
          <div class="pricing-body">
            <ul class="pricing-features">
              <li>Unlimited leads in pipeline</li>
              <li>Unlimited gigs on calendar</li>
              <li>All 4 outreach templates + custom</li>
              <li>Reply Hub — unlimited</li>
              <li>Artist assets tab</li>
              <li>Search + filter</li>
              <li>Auto follow-up scheduling</li>
              <li>Conversion funnel + Next Actions</li>
              <li>Custom follow-up intervals</li>
              <li>Cloud sync across devices</li>
            </ul>
            <a href="#" class="btn-primary" style="width: 100%; justify-content: center;">Upgrade to Pro →</a>
            <div style="text-align: center; font-size: 11px; color: var(--muted); margin-top: 12px;">Cancel anytime · No contracts</div>
          </div>
        </div>
      </div>

      <!-- Agency teaser -->
      <div style="max-width: 760px; margin: 20px auto 0; padding: 20px 28px; border: 1px solid var(--border); border-radius: 12px; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px;">
        <div>
          <div style="font-size: 13px; font-weight: 700; color: var(--text); margin-bottom: 3px;">Agency / Collective — coming Phase 3</div>
          <div style="font-size: 12px; color: var(--muted);">Multi-artist management, team access, shared pipeline · €29/mo</div>
        </div>
        <a href="#" style="font-size: 12px; color: var(--purple); text-decoration: none; font-weight: 600;">Join waitlist →</a>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="cta-section">
    <div style="position: relative; z-index: 1;">
      <p class="section-label" style="text-align: center; margin-bottom: 24px;">Ready?</p>
      <h2 style="font-family: 'Syne', sans-serif; font-size: clamp(36px, 5vw, 64px); font-weight: 800; letter-spacing: -0.04em; margin-bottom: 20px; line-height: 1.1;">
        Stop getting <em style="font-style:normal; color: var(--purple-l);">ignored.</em><br>Start getting <em style="font-style:normal; color: var(--gold);">booked.</em>
      </h2>
      <p style="font-size: 18px; color: var(--dim); margin-bottom: 40px;">From outreach to booked. Free to start.</p>
      <div class="cta-buttons">
        <a href="#" class="btn-primary" style="font-size: 16px; padding: 17px 36px;">Start for free →</a>
        <a href="#pricing" class="btn-ghost">See pricing</a>
      </div>
      <div class="cta-note">No account required · Data stays in your browser · Free forever</div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="container">
    <div class="footer-brand">
      <div class="nav-logo-bars" style="height: 18px;">
        <span></span><span></span><span></span><span></span>
      </div>
      <span style="font-family: 'Syne', sans-serif; font-weight: 800; font-size: 15px;">Stageflow</span>
      <span style="color: var(--muted); font-size: 13px; margin-left: 4px;">· Booking OS for DJs</span>
    </div>
    <div style="font-size: 12px; color: var(--muted);">"From outreach to booked."</div>
    <div class="footer-links">
      <a href="#">Privacy</a>
      <a href="#">Terms</a>
      <a href="mailto:hello@stageflow.io">Contact</a>
    </div>
  </footer>

  <script>
    // Sticky nav
    const nav = document.getElementById('nav');
    window.addEventListener('scroll', () => {
      nav.classList.toggle('scrolled', window.scrollY > 40);
    });

    // Fade up on scroll
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); } });
    }, { threshold: 0.1 });

    document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));

    // Animated stat numbers
    function animateNum(el, target, suffix, duration = 1200) {
      const isFloat = target % 1 !== 0;
      let start = null;
      const step = (ts) => {
        if (!start) start = ts;
        const progress = Math.min((ts - start) / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        const val = eased * target;
        el.innerHTML = (isFloat ? val.toFixed(1) : Math.round(val)) + '<span>' + suffix + '</span>';
        if (progress < 1) requestAnimationFrame(step);
      };
      requestAnimationFrame(step);
    }

    const statsObserver = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          const nums = e.target.querySelectorAll('.stat-num');
          const data = [[73,'%'],[3,'×'],[9,''],[0,'']];
          nums.forEach((el, i) => {
            if (i === 3) { el.innerHTML = '∞'; return; }
            animateNum(el, data[i][0], data[i][1]);
          });
          statsObserver.unobserve(e.target);
        }
      });
    }, { threshold: 0.5 });

    const statBar = document.querySelector('.stat-bar');
    if (statBar) statsObserver.observe(statBar);
  </script>
</body>
</html>
